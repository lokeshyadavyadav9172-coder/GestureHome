workflows:
  android-debug:
    name: GestureHome Android Debug
    max_build_duration: 60

    environment:
      java: 17

    scripts:
      - name: Build debug APK
        script: |
          gradle assembleDebug

    artifacts:
      - app/build/outputs/apk/debug/*.apk